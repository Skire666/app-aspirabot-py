------------------------------
# Sommaire
------------------------------

# HIGH
# MEDIUM
# LOW


------------------------------
# HIGH
------------------------------


A voir mais :
Pour le téléchargeur de SRt youtube :
- Filtrer SRT en double et garder les meilleurs qualités, parfois, ce sont les mêmes, mais il portent de noms différents.

Les 2 briques :
- split les 2 executors
- garder en cache les SRT, genre retenir l'URL, et ainsi pouvoir recycler. Faire une méthode globale qui push un cache
- attention, si SRT directe, sans ddl des infos avant, il faut le DDL entier, et quand même faire le SRT apres
- le monde extraire info, il n'exporte pas. Le mettre dans un extracted data + 
- model -> faire validation avec error, e^tre strict
- service -> faire validationIssue à la place du download_result, bourrer les erreurs. Pour les 2 services, être strict.
- faire une regle pour verifier qu'un scénario doit avec un export si une brique 'extraire***' existe (de n'importe quel type : extract link, text, ...)
Relges diverse rapide :
- Faure un log rapide qui print le temps pris pour la vérif, faire le time elapsed en milli-sec. j'ia besoin de métrique pour voir si ça fait des goulots d'étranglement.
- Le recommencer au début, doit toujours  être pistionné apres l'OPEN URL, et AVANT le kill browser. 

1) Scénario pratique : Je fait le ytb-dl normal, il échoue, je génère une fiche de bases quandmême dans le scéanrio,
2) le SRT, on s'en branle de la fiche, c'est juste un ddl de SRT. Par contre, renforcer le nommage (faut surtout pas que ça plante). Dans les params, indiquer cases à cocher pour prendre srt ENG et/ou FRA (qpres tout, sont filtré en dur). Puis pour l'export, numéroter les SRT 01,02,03, et se débrouiller pour prioriser les important (exemple : manuel sont plus qualitatif que les auto en principe, que les trads d'auto)



---------------

Extension avec ublock
Sur un nouveau PC (laptop)
le dossier n'existe pas, faire une doc pour isntaller projet
d'ailleurs, il faut python et je l'install jamais (je parle du binaire sur python.org)
mettre le dossier, mettre un warning si n'existe pas à l'utilisateur



---------------

Stabiliser le projet et la doc.
Arreter de théoriser sur les erreurs.
J'ai besoin de réfléchir, et surtout, de pratiquer, pour équiliber le trucs.

Faire quelques scénario :
- youtube - juste les nouvelle vidéo, sans srt (rajouter un check si vidéo n'est pas la)
- un youtube srt pour IA (est spécifique) + rajouter un check si vidéo n'est pas la
- solo srt manuelle (pas d'IA, juste liste à la mano)
- downnload soundtrack
- sountrack
- metacritics (jouer, lets go)
- allociné
- wawamnia, avec redirection

Faire wawacity qui change toute le temps de lien..
lorsque j'ouvre une url
il peut y avoir des redirections.
Notamment mes sites de piratage qui change tout le temps.

DDL de livre
E:\app-py-life-selector\src_python
dans l'idée faut extraire des datas, et push dans un CSV
mettre en clair aussi les images (pas dans le CSV ni base64) - penser à JPEG 50% (bourrin, mais osef)
a faire aussi, une sorte d'historique des liens visité, comme ça la fois d'apres, je visite les vieux liens
car apres, je vais remaj le truc


---------------

App-life-selector :
Faire un lecteur à part qui lit tous les JSON.
Le but c'est de faire mon app-life-selector.
Genre il construit un fichier final.
Je peux l'update avec une source (action manuelle, n'est pas auto).
Il est fait pureement acumulatif.
Il gère les doublons, ceux qui sont revisité.
Affiche l'historique si possible.
Attention, les exports JSON, genre youutve, parfois la clé n'est pas la.
Donc il faut gérer les valeurs par défaut et remplir les trous.
La question est de savoir par quoi ? Car bon, pour une valeur, je mets le MAX ou le MIN ?
Rendre çà configurable.

Ensuite, je me fait les priorités, date, populaire
et ce qui serait cool, c'est depouvoir choisr avec les touches du clavier si je garde  ou pas.
Tracer mes avis, changer l'ordre de mon heuristique.
POouvoir trier avec 1, 2, 3 critères comme excel.

---------------

Dans le repos
Push des exemple de scénario :
MAIS NE PAS PUSH LA PROD
Ce qu'il manque : une aide, pour savoir quoi saisir
gner des exemples en bas, rapide à copier et à adapter
faudrait un tuto

---------------



youtube
pas de feedback sur ce qui est téléchargé (srt, base)
Iniquer ENG et FRA explicitemetn dans les cases à cocher
dans l'IHM, à rajouter
le log aussi

---------------

Et quid de ytb-dl ? Si il échoue, je fais quoi... ?
Mon avis : si je ddl un SRT, c'est pour l'exploiter.
Notamment pour l'IA.
donc technique, s'il échoue, sert à rien d'avoir la fiche, car je vais jamais le mater.
Et si le SRT échoue, j'ai juste a retry avec un discover automatique.

---------------

Le bouton loading qie j'ai mis pour "Lancer le scraping"
si plus de 100 ms, il log une ligne ?
Histoire de tracer les events ?
Faire un is_busy aussi ?

---------------

Je sais pas quoi faire avec les erreurs :
D'un côté, planter tout le process, c'est con, je peux avoir 1 srt de raté sur 1000.
De l'autre, les gérer, c'est s'exposer à des passages sous silence d'erreur majeure.
Le pire, c'est d'exporter un truc qui est planté.
Parce que bon, gérer c'est une chose, mais pourrir la data, c'est horrible.
Je fais comment pour reprendre tout à la main ?
Ce qui est bien : Vérifier AVANT l'export JSON.
Genre renseigner les clés et chercher automatiquement 1 valeur ?
Et annuler ?
Genre je pourrais le mettre dans la GUI step, au moment de créer, en tant que paramètre, juste à lister les clés de gauche à droite.
Ensuite dans l'executor 

Idée : faire un fichier en plus du journal.
Prendre une URL, lister toutes les OK/KO de toutes les étpaes, indiquer nombre de fois ouvert, date création, date modification, et le faire en JSON (le CSV me démange, mais c'est pas générique...)

---------------

Lorque je suis en train d'éditer un scéanrio :
J'ai remarqué que pour avoir le détail d'une step
je faisais souvent "edit" en boucle dessus
au final, je ne lit pas la preview affiché dans la ligne.
Quoi faire pour faire moins de clique ?
Afficher plus dans la preview, quitte à supprimer les infos superflu (apres, tout, je lit 3 ou 4 informations, c'est rare de lire les 8 params à disposition)

V2 sympa :
Changer le workflow :
Lorque je fait un clic simple sur une ligne (pas un clic continu car sinon drag and drop)
Le mettre l'IHM en lecture seule, et si je clique sur "Modifer" il ouvre alors l'édition. Du coup, au global, moins d'aller retour, moins de clique.

---------------

Au dela du journal
faire un rapport final, dans un fichier a part, qui liste les statsitques
mais aussi les URL en erruer (juste en erreur)
Genre le journal fait le succès, et le report fait les erreurs.
il me faut un bilan des erreurs en fin
important que je regarde, et que j'ai un feedback
Genre 15000 youtube, 10 erreurs, c'est pas beaucoup, mais c'est peut anormal.

---------------

Mode pour le 'dossier JSON' :
Pour l'instant, j'ouvre JSON, et regarde partout les URLS.
Pouvoir configurer une clé, et un regexp URL.
Comme pour ce que je fais dans les découvertes automatiques.
Peut être pratique, même si j'en ia pas le besoin pour l'instant.

---------------

database sql ?
moins lourd que les JSON, et puis bon, c'est stabilisé
et pour commit, plus simple.
et au passage obfusque les URL problématiques...


------------------------------
# MEDIUM
------------------------------

---------------

Pour le menu "Modifier".
J'ai à chaque fois le cartouche en haut poru ajouter/modifier une étape.
En réalité, il faudrait l'intégrer inline, directement sous l'étape qu'on veut modifier.
Genre on clique sur le crayon, et il rajoute un cadre sous-ligne (et pousse les autres plus bas).
Pareille pour ajouter : il faudrait une ligne en surbrillance avec une sorte de "+" qui s'insère entre 2 lignes.
Avantage : Moins de distance pour la souris. Je vais aussi moins oublier que je suis en train d'éditer. Ajouter une étape se fera inline, et non pas à la fin (car dès fois, il m'arrive de les déplacer et les faire remonter).

---------------

L'optimisation du discover :
Faire un repository

Pour l'instant : 'update_sources_and_compute'
regarde si le model est le même (genre l'utilisateur n'a rien changé)
mais je regarde pas les fichiers.

Problème de base :
je regarde pas les dates des fichiers, Hors je peux bouger mes fichiers, et l'IHM ne sera pas avertie.
Il va donc rester avec les trucs en mémoire.
Je voulais optimiser le discover et ne pas recalculer, mais j'ai fait ça va vite.
A voir. En soit, faut vérifier si les inputs change, mais aussi les fichiers. Dans les 2 cas, recalculer.

---------------

youtube
dossier "./srt" en dur
on pourrait rajouter un champ
Le 'sanitize_youtube_url', il est en dur, faudrait documenter. Gère shot et vidéo et si suffixe fournir en plus (le '&')

---------------

Partie suite au retrait de "Version"
Faire court mais efficace :
site, usage, description
Limiter la taille des strings.

---------------

Nouvelle étape :
fait un snapshot du navigateur.
ça fait une capture de toutes la GUI.
Pour savoir si elle a bougé.

---------------

J'ai merdé les steps dans l'architecture.
C'est historique.
Les params sont pas hardcodé, et je passe mon temps à les copier/coller.
Les clés faut les durcifier.
Faire un type custom avec toutes les clés en dur.
Genre un dictionnaire typé.
le ranger dans un dossier 'shared/typing'

---------------

Pour la PROD :
script PS pour lancer l'applis sans VS code.

---------------

La boucle infinit du scrollbar vers la bas
Je voudrais pour le mettre en pause si jamais j'ai cliqué sur pause.
Mais dans l'étape, j'y arrive pas.
Je devrais demander à l'IA.
En profiter aussi pour trigger plus souvant, genre dans youtube.
Notamment dans une brique.
A noter que faire la chose suivante ne fonctionne pas : 'if not self._context.pause_event.is_set():'

---------------

Lors de l'édition d'un scénario, si je suis en train d'éditer une step :
si je quitte l'application, je détecte pas que je suis en train d'éfiter une étape, et du coup je sauve pas
SOLUTION : idéalement, faire un AppStateGlobal
EN profiter pour indiquer si 'is_busy' donc soit scraping, soit édition'?

---------------

Un jour, refaire le composant EditableTable et DataGrid.
Les 2 marchent, mais dans le détail, beaucoup de petit comportement qui manque ou des bugs.

Dans l'idée, faire un super composant qui refait tout en mieux, et ainsi remplacer ces 2 composants.
- Déjà, tout faire en canvas. Pas de composant déjà existant de tkinter.
- Le penser optimisé et demandé de lui faire un viewport pour ne calculer que la zone visible
Le tableau et les colonnes possèdent tous les flags pour l'initialiser et le paramétrer pour :
- redimensionnable ou pas, taille minimal (éviter de cacher une colonne)
- éditable ou pas, permet d'éditer du texte, tel un excel
- Style : Pouvoir changer la couleurs de l'en tête, la hauteur des lignes
- Pouvoir afficher séparateur de colonne, et de ligne (par défaut, ligne horizontale, pas de séparateur de colonne)
- scrollbar disponible (attention bugs : si on déplace la barre, les séparateur doivent suivre)
- le redimensionnemnt délcenche bien l'activation du scrollbar ou pas
- set_enabled pour tout griser
- changer le cursor en mode "arrow" pour redimnesionner un en-tête (si feature est activées)
- cursor hand pour cliquer sur un bouton ou un tête (et indiquer que c'est interactif à l'utilisateur)
- pouvoir clear tout le tableau en mémoire (le vider des éléments)
- pouvoir "bound" des élément
- Pouvoir récupérer une ligne, une sélection, ou alors tout le tableau entier, sans avoir à reconstruire les données
- couleurs configurable facilement (constante en haut du fichier).
- Déjà dit, mais tout en canvas (tkinter est tres limité en vrai)
- Bouton qu'on peut ajouter/masquer selon l'init :
- Rafraichir (appel le callback qu'on a décidé) pour actualiser le tableau (exemple, si la source c'est des fichiers)
- Bouton : Ajouter une ligne (si on peut éditer le tableau).
- Bouton : Supprimer une ligne (pouvoir l'activer)
- Bouton : Effacer tout le tableau 
- Pouvoir passer des actions personnalité à appliquer dans chauqe ligne (exemple, "Modifer")
- si action_browse, déclenche une action (browse un dossier), mais aussi remplit la target_key du tableau à remplir.
- Pouvoir afficher une erreur avec un trianle rouge.
- Afficher un compteur en bas, du nombre de lignes.
- Afficher une erreur globale sous le tableau (pour le feedback)
- Pouvoir nommer les colonnes mais aussi bind les clés techniques.
- pouvoir les cacher ou pas (les référencer, mais ne les montre pas, pratique pour les ID)
- Pouvoir indiquer manuellement si une colonne est éditable ou non.
- Si on clique sur une celle éditable, rentre en mode saisie.
- Pouvoir passer un converter qui change le format affiché (pratique pour date)
- Lorque j'ajoute une nouvelle ligne pouvoir passer un objet à utiliser par défaut.

---------------

------------------------------
# LOW
------------------------------

---------------

Refaire fichier @AGENTS claude. Et améliorer sa qualité.

Dans l'idée :
- Reprendre toutes les grandes lignes du fichier AGENT actuel.
- Refaire le SOLID aussi
- Insister sur les rôles, aide à la prise de décision pour trancher
- insister sur les tests unitaires
- urff, basedpyright, pytest
- toute le temps des properties, pas d'acces directe
- faire ma nouvelle gestion d'erruer, avec le ErrorCode (NOTE PCO : changer le validate, et pouvoir passer un contexte, car quand je valide les lignes d'un tableau, je sais pas la ligne qui plante + faire comme systex avec un is_applicable et un tableau pour chainer la validationn comme mon pointeur de fonction)

Model, faire un héritage avec une des interfaces suivantes :
- Si CRUD, tout le temps date created, date modified, tout le temps ID, mark ascreateD/modified
- Si collection de CRUD, comme StepsCollection 
- to_dict, from_dict, clear (pas de default, le default c'est service qui crée), validate
Le service :

Le ViewModel aussi :
- faire une méthode 'snapshot' qui convertit l'UX/UI en model, set_enabled -> DISABLED UX/UI, un clear qui vide l'IHM, faire un refresh qui prends un enum (et sélectionne qui refrachit quoi), faire un is_dirty ainsi qu'un is_busy


---------------

C_LIMIT_GIVE_UP_SCROLLING 
2 fois
Est en dur
faudrait le rendre configurable dans le ScrollDown* (IHM, model, etc..)

---------------

Au lieu de faire une truc débile qui suffixe un ID pour le profil ou le scénario
Faire simplement un dossier.

---------------

timeout en dur :
"socket_timeout": 30,
Pas bien.

---------------

Si un jour prolbème d'optimisation JSON car j'en lit beaucoup :
msgspec Struct (zéro-copie)
msgspec  (sérialisation/désérialisation zéro-copie via Struct)
Le plus rapide : schema défini → décodage typé en une passe.
cf internet

---------------

Faire la config en singleton.
Et tout le monde pioche dedans.
Vraiment chiant de MAJ toutes les couches.

---------------

si je supprime un scéario,
je supprime tous les profils,
mais je préviens pas du nombre de profil, pourtant il peut y avoir des dépendances.
le truc c'est que j'ai plein d'URL, et je peu xpas les réassinger.
c'est dommage, même siu pas grave de les perdre.

---------------

erreur inline
un jour, faire comme EC SAL :
erros_lines.py
En profiter aussi pour faire un composant générique "Ligne" qui permet de push des contrôles
ensuite je push une ligne
ovveride tout tkinter à force.

---------------

Pour le FUN :
- Fichier de configuration, et dedans, mettre la taille de la police et la typo.
- Pouvoir la modifier, genre augmenter la taille, commen dans les jeux vidéo.

---------------

Lorsque je fais un edit :
toujours créer un fichier temp, et ensuite rename.
comme ça, si echec durant l'écriture, ou l'application se quitte.
je ne perds pas le fichier, et il reste planté sur l'ancien.
En plus, si je reprends, je peux regarder s'il reste un travail en cours.

---------------

# EOF
