------------------------------
# Sommaire
------------------------------

# HIGH
# MEDIUM
# LOW


------------------------------
# HIGH
------------------------------

--------------

La gestion des erreurs dans une étape.
Du mieux, mais ça reste pénible.

Déjà, il faudrait pouvoir suppress de manière arbitraire un code d'erreur.
Genre 'YSL_1002'
Parce que j'ai c'est bien jolie de faire des erreurs non gérée à cooup de if, mais d_è_s fois, je peux juste rien y faire.
Typiquement, sur une vidéo youtube, il trouve ni sous-titres ni langues, car la vidéo à 7 ans, et donc y'a rien à prendre en plus.


Ensuite, je renvoie des succes ou des erreurs.
Il faudrait renvoyer des warnings aussi (je les passes sous silences)
Par exmeple, je passe sous silence 'YIV_1006'

Ensuite, ne plus renvoyer un état, mais bien un validationResult.
Un résultat succès/erreir ne permet pas d'extrapoler la nature de l'erreur ou du succès.
Juste ça '-1' alors même qu'il y a des '-1' plus grave que d'autre.
Pareille pour les warnings.

Moins faire d'exception aussi, plus d'enum.
Déjà ça facilite la gestion locale.
De plus, me permet d'identifier plus finement une erreur et potentiellemet changer de comportement.
Me force à gérer le retour explicitement
Moins d'exception qui viennet me casser la callstack et que je ne logs jamais tres proprement
bre,f plus explicite et mieux géré
Puis en plus va méviter les trucs déguex genre "[0;31mERROR:[0m [youtube] rRocl7iqFPE: Video unavailable. This video is not available"

Afficher une statistiques : 
- Les erreurs les plus freéuqnete, genre undictionnaire, je push les str des erreurs, et je fais +1



--------------

Un jour, faire un extractor dédié.

Dans l'idée : Dans mon scénario, j'ajoute UNE SEULE brique extractor.
Cette extractor est un panneau dédié.

Dedans, il y une liste de sous-extractor (exemple ave youtub) :
- les infos basique
- le yt-dlp complet
- les SRT.

Je peux ajouter un sous-extractor comme je le souhaite :
- Chacun export/extract gère ses propres colonnes
- Chacun à sa propre méta données
- Un sous-extractor possède un préfixe, et chaque colonne est préfixé automatiquement avec.

Ainsi :
- Les extractors s'éxécute dans l'ordre.
- Chacun peut faire des vérifs basiques avant (genre si la page est la, telle valeur bien disponible, etc...) le chargement d'une page.
- Pour chacun extractor, je peux mettre des pré-conditions de page. Pratique si page pas chargé, ou si la présence de la donnée est parfois optionnelle
- Les colonnes des extracteurs sont toujours "rangé" dans osn préfixe, ainsi, je peux avoir plusieurs "date_created" car chacun extractor va faire "groupe_a.date_created", "groupe_b.date_created", "groupe_c.date_created", donc je les distingues tous.
- chacun a ses meta-données: date_created, date_modified, date_session, quality_row, quality_date, priority_rank, etc... Chaque extractor à ses propres-métadonnées.
- Le bilan complet me permet de voir toutes les futurs colonnes que va posséder mon CSV (et donce vois mieux les métadonnées).
- Je peux aussi renforcer les sécurités en sortie de l'extractor, et ainsi remonter une erreur ou cancel si j'estime qu'il y a trop de données vide, etc... (compter les cellules vides, etc...)

Avantage :
1) Comme j'ai plus de méta-données, et que je regarde la qualité du remplissage, avoir des dates vides pour un extractor est censer le pénaliser, et donc faire remonter sa priorité à le traiter pour le prochain coup (peut être qu'il ne va pas se remplir, mais au moins, les neufs verront leurs date alimentés, ce qui montre au minimum qu'il sont passés dans la moulinette, à défaut d'être parfait). Pour rappel, le score des priorités et la combinaison "nombre de cases remplis" + MAj de la dernière session de scraping (si récent, pas besoin de le refaire).

2) Pouvoir valider la cohérence d'un CSV que j'ouvre.
Regarder que les colonnes sont bien présentes et corresponde bien au model défini logicielement (parfois, je modifie le CSV à l'extérieur).
Je pense notamment à mon app-life-selector qui pourra le modifier de l'extérieur, avec ses propres colonnes.

3) Mettre en place des heurisitques et calculer la qualité de la data dans un CSV.
Typiquement, la moyenne des cases remplits, l'age moyen des datas, etc...
Le but étant d'avoir des indicateurs à mêmes de m'orienter sur ce que je dois prioriser pour augmenter la qualité.

4) Lorque je launch un scénario pour le scrapping, et qu'il est cablé sur un scraping.
Je peux choisir les dates (et comme j'ai des groupes qui préfixe les dates, je peux choisir le create de l'extractor, le create de l'extractor 2, etc...)

5) Comme je fais une vérification en sortie, ça veut dire que je peux cancel le push d'une data.
Typiquement, j'ai identifié trop de probleme sur une date (genre les 3/4 sont vides),  un probleme réseaux, durant la nuit, je me lève le matin, il a écraser les données.
NOTE PCO : push une donnée uniquement si pas vide ? histoire de ne jamais écraser une ancienne ?


NOTE : sur la qualité de la donnée :
Faut faire avec je pense. Typiquement, youtube, des vidéos sont restrreint à un age puis bloqué, et vice versa.
Soumis à un age, soumis à un paywall, vidéo supprimé, ou tout simplement je me prends un timeout réseaux.
Tout ces cas d'usage peuvent aboutir à une régression des données (genre le vidéo est accesible, j'ai toutes les infos, puis la vidéo disparait, j'update alors la data et je perds toutes mes avancées).
Donc je pense que viser 95% de qualité, de remplit, et avoir le plus de données MAJ (genre tous date de modifed sont proches de datetime.now, ce qui revient à dire que la dataset se rapproche au mieux des données actuelles/réelles.)

--------------

Pour le rapport final, et l'amélioration des feedback pour les erreurs.
Je ne lit plus jamais le report.
Donc déjà, il s'accumule, il faudrait faire le ménage (ou pas)
Mettre aussi plus l'emphase les erreurs dans aspirabot.
Genre afficher un rapport final, déguelasse, mais qui affiche les erreurs + l'URL en cause ?

Pour l'export dans le CSV : éviter de push les datas plantés.
C'est déjà chiant à gérer.
Par contre, je pourrais faire un rapport d'erreur à part.
Ne le générer que si des erreurs, et si tout est OK, supprimer les derniers logs (mais en garder toujours 1 ?)

--------------

Un jour : sortir le nom de l'export et rendre ça accessible dans le réglage des URLS.
Dans l'idée, vérifier si t'a des briques extract, et si oui, rendre effective la modification du champ pour choisr un emplacement pour le CSV de sortie.
En l'état, obligé de garder la brique export, car le nom est dedans, et techniquement, mon launcher n'a pas la vision de l'export.
Cependant, avec les sécurités, et en lisant les infos du scéanrio, je pourrais modifier/gérer ça dans le launcher.

A noter :
export variable systme, ne sert plus à rien
mais ça prends du temps à enlever, et j'ai pas envie de le recabler plus tard.
Pour l'instant, ne me bvloque pas, le laisser.
Attendre d'avoir pratiqué le logiciel pour savoir ce dont je me sers vraiment ou pas.

--------------

faire des exmeples de template de scénario
sushiscan,
readcomics
update dossier.
etc...

Usecase :
Pouvoir lancer les 5 gliesnde sppringan, en 1 clic.

- Sprigan, tu lance, avec X liens (soit pioche un dossier, soit ouvre un txt, soit input manuel). A noter qu'il faudrait au début créer un contexte, mais n'a pas de page. C'est l'ouvrir un URL qui fait ça si 0 pages.
- Fait un historique des trucs run, comme ça, tu reouvre l'appli, et tu relance sans tout restup
- La cartouche information, le refaire. Mettre nom, description, version?
- Faire un debugger de css/selector, genre simple : debug un chrome qui persiste, affiche HTML (actua à la mano), et tu lance un css/selector et te display un output génétique 'inntext, outertext', et si image, inaffiche src, width et width natural
- Plus de fin de processus (par contre, mettre une case qui dit laisser ouvert à la fin)
- Brique Condition : plus de jump to. Faire conidtion succes/echec, jump, ou fin processus
- Télécharger image, rajouter si doit prendre doublons, si 0 téléchargé, raise une error. Penser aussi à faire un service 'service_downloader' qui gère l'output du fournisseurs.
- avant de run un workflow, renseigner dossier de sortie, save ça dans le fournisseurs.
- Le fichier de config, ne fait plus d'output, supprimer
- et faire un outil shared pour Pathfile, je comprends rien au comporement de makedirs, name, etc... encapsule



temaplte metacritcs
auto read devianrt, multi page
youtube suvtitle en dur
youtrube subtitle via dlèyb
metacritics, homage JV
metacrtics, year page 1, page 2, etc.. fail.
MAj dossie rE:/app-py-life-selector/checker truc piraté (et non extracteur) - juste MAJ dernier clique/004 - e-hentai.org


j'yrepense, mais diée trop cool
téléchager iamge en boucle
en mêm telos que je lit
c'est théorqiuemetn possible


Faisons le discover youtube.
Et ensuite metacritics : https://www.metacritic.com/game/   https://www.metacritic.com/browse/game/
et zlib. Un bon crashtest.

https://www.goodreads.com/author/show/656983.J_R_R_Tolkien
https://www.goodreads.com/list/show/6.Best_Books_of_the_20th_Century

discver avec a[href^="/game/"] marche bien.



Faut vider DDL, srrigan est fait, il faut UNI.

!

Quid d'un site qui change, genre le domaine, ou la structure HTML.
Il me faudrait faire des workflow de test.
Et voir si le scenario est toujours OK.
Histoire de voir les régressions (structure d'un site qui change, le rename, etc...)
Par contre, ça voudrait pouvoir tester  que mon workflow ne lance plus une URL en dur.
Mais prendré un inpout, comme un flux text, CSV, ou autre.


Use case :
Flague mes données avec mes avis persos.
Systeme de tag, marche bien
Faire ton score d'affinité
et consommer... et donc pouvoir étiquetter ce que tu consome, archive, etc...

Stabiliser le projet et la doc.
Arreter de théoriser sur les erreurs.
J'ai besoin de réfléchir, et surtout, de pratiquer, pour équiliber le trucs.

Faire quelques scénario :
- youtube - juste les nouvelle vidéo, sans srt (rajouter un check si vidéo n'est pas la)
- un youtube srt pour IA (est spécifique) + rajouter un check si vidéo n'est pas la
- solo srt manuelle (pas d'IA, juste liste à la mano)
- downnload soundtrack
- sountrack
- metacritics (jouer, lets go)
- allociné
- wawamnia, avec redirection

Faire wawacity qui change toute le temps de lien..
lorsque j'ouvre une url
il peut y avoir des redirections.
Notamment mes sites de piratage qui change tout le temps.

DDL de livre
E:\app-py-life-selector\src_python
dans l'idée faut extraire des datas, et push dans un CSV
mettre en clair aussi les images (pas dans le CSV ni base64) - penser à JPEG 50% (bourrin, mais osef)
a faire aussi, une sorte d'historique des liens visité, comme ça la fois d'apres, je visite les vieux liens
car apres, je vais remaj le truc


---------------

App-life-selector :
Faire un lecteur à part qui lit tous les JSON.
Le but c'est de faire mon app-life-selector.
Genre il construit un fichier final.
Je peux l'update avec une source (action manuelle, n'est pas auto).
Il est fait pureement acumulatif.
Il gère les doublons, ceux qui sont revisité.
Affiche l'historique si possible.
Attention, les exports JSON, genre youutve, parfois la clé n'est pas la.
Donc il faut gérer les valeurs par défaut et remplir les trous.
La question est de savoir par quoi ? Car bon, pour une valeur, je mets le MAX ou le MIN ?
Rendre çà configurable.

Ensuite, je me fait les priorités, date, populaire
et ce qui serait cool, c'est depouvoir choisr avec les touches du clavier si je garde  ou pas.
Tracer mes avis, changer l'ordre de mon heuristique.
POouvoir trier avec 1, 2, 3 critères comme excel.


yt-dlp
Valider le model
ntoamment si des valeurs vides
renforcer le code.

---------------

Ce qui serait bien, c'est un extracteur pour une page.
Genre au lieu d'extraire 1 à 1 avec mes briques EXTRACT puis export.
Tout simplement déclarer dans une page à part, un menu extracteur.
Genre me fait un plugin générique extract une page spécifique (je repense à mon dispatcher...).
je déclare, toutes mes CSS selector. J'en profite au passage pour me donner la capacité de déclarer des attributes (href est un attribut, et certain trucs d'accessbilité sont dans les attributes aussi, donc pratique en vrai de pouvoir prendre un attribute au minimum).
Et ainsi faire : document.querySelector('.ytwFactoidRendererFactoid').getAttribute('aria-label');
je garde toujours la notion de inner,outer,textcontent,value ...
Je déclare au passag explicitement des colonnes de bases comme "_primary_key__", "__date_created__", etc....
Une fois que j'ai déclaré cette extracteur. Je le rends disponible dans une brique "EXTRACT"

Ce qu'on peut en faire :
- Attendre explicitement certaines clé en entreé (genre cocher si on doit attendre la présence de cette élément, en mode obligatoire), evitera par exmeple les problemes de chargmeent de page.
- faire les attribute (déja indiqué)
- Indiquer les varaibles optionnels (genre comme les tags qui n'existe pas)
- mettre des valeurs par défaut (compteur de vidéo à -1, nombre de like à -1, etc....)

Débat :
J'ia demandé à l'IA de me faire un javascript pour extraire les infos d'une page youtube
ça marhce plutôt bien, et en plus, il suffit d'injnecter le JS directe
ensuite, jsutre à récupérer un dictionnaire python.
En plus, dans debug, je pourrais exécuter le javascript.<

Pour tests les changements/regressio d'un site :
Prendre une page populaire, genre gangnam style, et exécuter le script ?
Regarder si les valeur sont aliemntés.

Ce que ça change :
- menu explicitie pour extract de varaible et mes extracts de textes.
- Mes autres extractor comme "links" et "yt-dlp" sont déjà des etracteurs disponibles
- Je n'ai plus plusieurs briques extract, mais 1 seul.
- C'est un peu comme j'avais des briques par défaut pour extraire, et  des briques custom.
- Je pourrais indiquer la source, ce qui en plus limite le nombre d'appel, et réduit le nombre d'étape.
- Je peux réutiliser les extractors dans les scénarions. Il fuat les voirs comme des plugins.
- ça m'aidera a gérer le fait d'avoir un extracteur youtube complet, ou extracteur dégradé quand la vidéo est bloqué.
- moins d'étape aussi dans le scéanrio, et meilleur visibilité en un coup (plutôt que de cliquer edit partout)

---------------


zlib
probleme : je clkique, il téléchgarge en temporaire dans 
C:\Users\Luzeur7692\AppData\Local\Temp\playwright-artifacts-4QNlvD
alors oui je gagne du temps dans l'ensemble (120 lien en 1 heure)
mais fondamentalement, je ne fias qu'ouvrir des URLs en boucle.
Je pourrais clairement discover, listeer les liens
Ensuite je clique manuellement les liens dans le vrai navigateur
Probleme, j'ai pas les liens "cliquable"

Autre idée :
faire un mode discover, mais qui push un v1 lite dans une BDD
2eme mode : Le selector de la fiche fait un discover v2 fat
3eme mode : Le checking (aka lire dossier), ne push rien, par contre, dans ce mode, je peux cliquer, sur une URL, et il 


Auitre probleme :
Pour choper les novueaux liens à télécharger
ça fonctionne sur le tableau suite à la recherche
<span class="mark "><span class="label" style="opacity: 1;">Downloaded</span></span>
<span class="mark downloaded"><span class="label" style="opacity: 1;">Downloaded</span></span>

Mais çamarhce pas sur les listes personnelles :
https://z-lib.fm/booklist/3056318/157bc4/kokato-download-2.html

Donc je repense à mon dispatcher, qui était censé sélectionné le bon scénario.
Genre il ouvre l'URL.
Il dispatch sous un sous neoud, il le traite
il ouvre l'URL suivante





---------------


Si je change l'emplacement des scénarios dans l'option config
je save
je reviens sur la panel des scénarios, le refresh ne load pas le nouvel emplacement


---------------

App-life-selector
affinitré selon gout
date ancien à prioriser
nombre d'heure
prix actuelle le plus rentable ?

---------------


C:\Users\Luzeur7692\AppData\Local\Temp\playwright-artifacts-4QNlvD
Je fais zlib, il ddl tout en TMP.
L'enfer.

Faire un hyperling qui affiche l'emplacement et qui permet de récéuprer les trucs.


---------------





Faire une capture d'écran apres ouverture de la page ?
Comme ça je sais le dernier truc que j'ai visiter sur downloadsounctrack, siitginincloud, etc...

---------------

ddl transcript
faire clsutergin ,vector machin truc

https://claude.ai/chat/9a0f5a56-14ed-4dd9-b1f1-c33c1960ec74

---------------

Idée en en brainstorm, mais c'est à réfléchir.
Le JSON, si 10000 fichiers, est longs à lire, couteux à parser, et potentiellement verbeux alors même que la structure ne change pas.
Ce qui serait bien :
- BDD ? Ou alors stocker en CSV. Genre ne plus faire 1000 JSON, mais un tabulaire/CSV qui centralise tout.
- Contrainte : Pouvoir définir à l'avance ma structure de BDD, genre mes colonnes, mes champs, etc...
- Ensuite, dans mon scénarion, avec les extract, je bind pas une key, mais une des éléments de ma structure que j'ai pré-remplit. genre obligé de sélectionner une entrée pré-existantante de ma BDD.
- L'export, il push une ligne en BDD, simple, efficace.
- Avantage : Je ne repète pas ma structure dans chaque fichier, donc gain d'espace. Pas le surcout d'ouvrir X fichier, un seeule lecture. Plus facile à manipuler une fois intégralement en mémoire.

Ce qu'il faut décider : le JSON est en clair, et malgré tout, c'est pratique. MEttre dans un CSV permet de garantir ça, mais implique de changer la philosophie de lecture est d'écriture, soit une refonte lourd potentiellement.

Idée cool en tout cas : Lire une colonne à un extract.

Autre aspect cool : Scrrol vers le bas 1 fois la page
et push les 10 nouvelles URL dans la BDD
rescroll la page (sans changer) et push à nouveau les 10.


---------------

Débat :
Beaucoup de clique pour MAJ
C'est pas ce que j'avais prévu.
Je repense à mon dispatcher.
Genre il trouve des URLS, les découvre, prends la fiche.
La je discover totu, puis update les 10 000 fiches.
C'est con, je devrais pouvoir lancer mes process journalier, tous, pas 1 à 1



---------------

Dans le repos
Push des exemple de scénario :
MAIS NE PAS PUSH LA PROD
Ce qu'il manque : une aide, pour savoir quoi saisir
gner des exemples en bas, rapide à copier et à adapter
faudrait un tuto

---------------

youtube
pas de feedback sur ce qui est téléchargé (srt, base)
Iniquer ENG et FRA explicitemetn dans les cases à cocher
dans l'IHM, à rajouter
le log aussi

---------------

Le bouton loading qie j'ai mis pour "Lancer le scraping"
si plus de 100 ms, il log une ligne ?
Histoire de tracer les events ?
Faire un is_busy aussi ?

---------------

Je sais pas quoi faire avec les erreurs :
D'un côté, planter tout le process, c'est con, je peux avoir 1 srt de raté sur 1000.
De l'autre, les gérer, c'est s'exposer à des passages sous silence d'erreur majeure.
Le pire, c'est d'exporter un truc qui est planté.
Parce que bon, gérer c'est une chose, mais pourrir la data, c'est horrible.
Je fais comment pour reprendre tout à la main ?
Ce qui est bien : Vérifier AVANT l'export JSON.
Genre renseigner les clés et chercher automatiquement 1 valeur ?
Et annuler ?
Genre je pourrais le mettre dans la GUI step, au moment de créer, en tant que paramètre, juste à lister les clés de gauche à droite.
Ensuite dans l'executor 

Idée : faire un fichier en plus du journal.
Prendre une URL, lister toutes les OK/KO de toutes les étpaes, indiquer nombre de fois ouvert, date création, date modification, et le faire en JSON (le CSV me démange, mais c'est pas générique...)

---------------

Lorque je suis en train d'éditer un scéanrio :
J'ai remarqué que pour avoir le détail d'une step
je faisais souvent "edit" en boucle dessus
au final, je ne lit pas la preview affiché dans la ligne.
Quoi faire pour faire moins de clique ?
Afficher plus dans la preview, quitte à supprimer les infos superflu (apres, tout, je lit 3 ou 4 informations, c'est rare de lire les 8 params à disposition)

V2 sympa :
Changer le workflow :
Lorque je fait un clic simple sur une ligne (pas un clic continu car sinon drag and drop)
Le mettre l'IHM en lecture seule, et si je clique sur "Modifer" il ouvre alors l'édition. Du coup, au global, moins d'aller retour, moins de clique.

---------------
Mode dossier JSON
Faire un bouton qui affiche tout
toutes les URLS, leurs date
Je manque de feedback, me^me si j'ai vérifié que ce code fonctionne

---------------

Au dela du journal
faire un rapport final, dans un fichier a part, qui liste les statsitques
mais aussi les URL en erruer (juste en erreur)
Genre le journal fait le succès, et le report fait les erreurs.
il me faut un bilan des erreurs en fin
important que je regarde, et que j'ai un feedback
Genre 15000 youtube, 10 erreurs, c'est pas beaucoup, mais c'est peut anormal.

---------------

Mode pour le 'dossier JSON' :
Pour l'instant, j'ouvre JSON, et regarde partout les URLS.
Pouvoir configurer une clé, et un regexp URL.
Comme pour ce que je fais dans les découvertes automatiques.
Peut être pratique, même si j'en ia pas le besoin pour l'instant.

---------------

database sql ?
moins lourd que les JSON, et puis bon, c'est stabilisé
et pour commit, plus simple.
et au passage obfusque les URL problématiques...

---------------

Cas d'usage à débattre, mais je suis pas sur de l'intéret.
Des fois, genre youyube, je liste sur la chaines toutes les vidéos.
Certe, je peux prendre les liens, et discover ensuite video par vidéo.
Mais je peux aussi résonner autrement :
Si je focus la grille qui liste tous les éléments/vidéo, je peux déjà récupérer des liens, des titres, des compteurs. Et du coup, peut être pas lire video par vidéo.
Problème 1 : En l'état, je peux difficilement extraire une sous-page (idée déjà eu, genre je coupe et garde une partie de la sous-page, et ainsi éviter des faux positifs, facilite mes selector, le découpage propre, item par item).
Problème 2 : Une fois que j'ai la grille, je peux découper en élément générique. Pour l'instant, je peux pas faire ça, car lorsque j'extrait un selecteur CSS "clé 1", il va récupère tous la liste, et ainsi de suite avec les autres clés, mais du coup, j'ai pas les clé 1, 2 3, pas vidéo, mais toutes les clés 1, puis les clés 2, sans répartition par vidéo.

---------------

Probleme futur : devoir update de manière daily, 3000 vidéos par jour... Compliqué de tout visiter. Puis en plus, si un site update, et casse le parsing, je doit MAJ le scénario. Et si tous les jours un site casse, je passe ma vie a update.

------------------------------
# MEDIUM
------------------------------

---------------

Pour le menu "Modifier".
J'ai à chaque fois le cartouche en haut poru ajouter/modifier une étape.
En réalité, il faudrait l'intégrer inline, directement sous l'étape qu'on veut modifier.
Genre on clique sur le crayon, et il rajoute un cadre sous-ligne (et pousse les autres plus bas).
Pareille pour ajouter : il faudrait une ligne en surbrillance avec une sorte de "+" qui s'insère entre 2 lignes.
Avantage : Moins de distance pour la souris. Je vais aussi moins oublier que je suis en train d'éditer. Ajouter une étape se fera inline, et non pas à la fin (car dès fois, il m'arrive de les déplacer et les faire remonter).

---------------

L'optimisation du discover :
Faire un repository

Pour l'instant : 'update_sources_and_compute'
regarde si le model est le même (genre l'utilisateur n'a rien changé)
mais je regarde pas les fichiers.

Problème de base :
je regarde pas les dates des fichiers, Hors je peux bouger mes fichiers, et l'IHM ne sera pas avertie.
Il va donc rester avec les trucs en mémoire.
Je voulais optimiser le discover et ne pas recalculer, mais j'ai fait ça va vite.
A voir. En soit, faut vérifier si les inputs change, mais aussi les fichiers. Dans les 2 cas, recalculer.

---------------

Au démarrage, check les fichiers
créer par défuat les profiles si pas déjà là avec un scéanrion.
En profiter aussi pour vérifer qu'on peut écrire (disque dur plein, jamais géré)
Vérification :
Faut blinder, au chargement, tout lire.
genre tous le model, avec toutes les verifs, y compris toutes les variables.
process de début, process de fin. Obligatoire.
le faire udrant splashscreen
Vérifer si splashscreen bug pas si process trop long

---------------

youtube
dossier "./srt" en dur
on pourrait rajouter un champ
Le 'sanitize_youtube_url', il est en dur, faudrait documenter. Gère shot et vidéo et si suffixe fournir en plus (le '&')

---------------

Partie suite au retrait de "Version"
Faire court mais efficace :
site, usage, description
Limiter la taille des strings.

---------------

Le cache de mes repository :
j'ai remarqué que c'était aps optimisé
genre des qu'il copie, il devient plus longs.
C'est assez bizzare.
Alors oui j'ouvre pas le fichier, mais fuite mémoire potentiellement, surtout si gros fichier, et en plus n'accélère pas, ralentit.
A voir si je peux amélioré, en vrai pas facile, mon code base + oprtim via IA n'a rien amélioré.
Peut être qu'il faudrait renvoyer l'objet model ?
Mais quid de s'il est modifié quelque part ?

---------------

Nouvelle étape :
fait un snapshot du navigateur.
ça fait une capture de toutes la GUI.
Pour savoir si elle a bougé.

---------------

les 1ère couche de repository
refaire le service workflow
GenDI dedans. Ne plus par random utile.
C'est le service qui doit gérer.


---------------

J'ai merdé les steps dans l'architecture.
C'est historique.
Les params sont pas hardcodé, et je passe mon temps à les copier/coller.
Les clés faut les durcifier.
Faire un type custom avec toutes les clés en dur.
Genre un dictionnaire typé.
le ranger dans un dossier 'shared/typing'

---------------

Pour la PROD :
script PS pour lancer l'applis sans VS code.

---------------

La boucle infinit du scrollbar vers la bas
Je voudrais pour le mettre en pause si jamais j'ai cliqué sur pause.
Mais dans l'étape, j'y arrive pas.
Je devrais demander à l'IA.
En profiter aussi pour trigger plus souvant, genre dans youtube.
Notamment dans une brique.
A noter que faire la chose suivante ne fonctionne pas : 'if not self._context.pause_event.is_set():'

---------------

Lors de l'édition d'un scénario, si je suis en train d'éditer une step :
si je quitte l'application, je détecte pas que je suis en train d'éfiter une étape, et du coup je sauve pas
SOLUTION : idéalement, faire un AppStateGlobal
EN profiter pour indiquer si 'is_busy' donc soit scraping, soit édition'?


---------------

app-config
le service, faire un singleton pour y accéder.

---------------

Les CSS selector dans l'IHM
trop long a affiché
cut le surplus, et garder genre 40 ?

---------------

UX/UI :
IDée : Groupe les briques par catégorie, avoir un onglet par catégorie.
Mettre icones ?


---------------

Modifier le workflow :
rajouter un set_enabled, qui grise la liste CRUD
le temps de modifier une step.
Si j'édite un truc, et que je drag and drop
NOTE PCO : Faire un is_busy / is_edit
faire un singleton view state, bourrin, mais pratique.
Mineur : J'indique vérification apres MAJ ou création de step
Par contre, le duppliquer, supprimer, devrait trigger aussi.
Mettre ça sur le trigger du compteur d'éléments ?
Ancien prompt utilisé :
- Lors de l'utilisation du bouton 'self._btn_confirm' (soit en mode "Ajouter l'étape" ou "Mettre à jour")
- Effectue la vérification complète de la liste des étapes (avec "self._workflow_service.validate_step") et affiche un message à gauche du bouton 'Sauvegarder les fournisseurs'.


---------------

Un jour, refaire le composant EditableTable et DataGrid.
Les 2 marchent, mais dans le détail, beaucoup de petit comportement qui manque ou des bugs.

Dans l'idée, faire un super composant qui refait tout en mieux, et ainsi remplacer ces 2 composants.
- Déjà, tout faire en canvas. Pas de composant déjà existant de tkinter.
- Le penser optimisé et demandé de lui faire un viewport pour ne calculer que la zone visible
Le tableau et les colonnes possèdent tous les flags pour l'initialiser et le paramétrer pour :
- redimensionnable ou pas, taille minimal (éviter de cacher une colonne)
- éditable ou pas, permet d'éditer du texte, tel un excel
- Style : Pouvoir changer la couleurs de l'en tête, la hauteur des lignes
- Pouvoir afficher séparateur de colonne, et de ligne (par défaut, ligne horizontale, pas de séparateur de colonne)
- scrollbar disponible (attention bugs : si on déplace la barre, les séparateur doivent suivre)
- le redimensionnemnt délcenche bien l'activation du scrollbar ou pas
- set_enabled pour tout griser
- changer le cursor en mode "arrow" pour redimnesionner un en-tête (si feature est activées)
- cursor hand pour cliquer sur un bouton ou un tête (et indiquer que c'est interactif à l'utilisateur)
- pouvoir clear tout le tableau en mémoire (le vider des éléments)
- pouvoir "bound" des élément
- Pouvoir récupérer une ligne, une sélection, ou alors tout le tableau entier, sans avoir à reconstruire les données
- couleurs configurable facilement (constante en haut du fichier).
- Déjà dit, mais tout en canvas (tkinter est tres limité en vrai)
- Bouton qu'on peut ajouter/masquer selon l'init :
- Rafraichir (appel le callback qu'on a décidé) pour actualiser le tableau (exemple, si la source c'est des fichiers)
- Bouton : Ajouter une ligne (si on peut éditer le tableau).
- Bouton : Supprimer une ligne (pouvoir l'activer)
- Bouton : Effacer tout le tableau 
- Pouvoir passer des actions personnalité à appliquer dans chauqe ligne (exemple, "Modifer")
- si action_browse, déclenche une action (browse un dossier), mais aussi remplit la target_key du tableau à remplir.
- Pouvoir afficher une erreur avec un trianle rouge.
- Afficher un compteur en bas, du nombre de lignes.
- Afficher une erreur globale sous le tableau (pour le feedback)
- Pouvoir nommer les colonnes mais aussi bind les clés techniques.
- pouvoir les cacher ou pas (les référencer, mais ne les montre pas, pratique pour les ID)
- Pouvoir indiquer manuellement si une colonne est éditable ou non.
- Si on clique sur une celle éditable, rentre en mode saisie.
- Pouvoir passer un converter qui change le format affiché (pratique pour date)
- Lorque j'ajoute une nouvelle ligne pouvoir passer un objet à utiliser par défaut.

---------------

------------------------------
# LOW
------------------------------


penser à duppliquer le 'profil de lancement'
penser à faire un profil par défaut lors d'une création.
exécuter, si je sélectionne, et pas de profil en créer par défaut.
Nouveau, renommer, sauvegatrder, deete profil du scrapping

---------------

Optimisation 
Pour le téléchargeur de SRT youtube :
- Filtrer SRT en double et garder les meilleurs qualités, parfois, ce sont les mêmes, mais il portent de noms différents.

---------------

Lorsque lit RAC :
UTF-8, fichier mal formaté, caractères spéciaux
Mettre une vérification à l'utilisateur, car en l'état, si le truc est en erreur, il l'ignore, et on sait jamais pourquoi.

---------------

Refaire fichier @AGENTS claude. Et améliorer sa qualité.

Dans l'idée :
- Reprendre toutes les grandes lignes du fichier AGENT actuel.
- Refaire le SOLID aussi
- Insister sur les rôles, aide à la prise de décision pour trancher
- insister sur les tests unitaires
- urff, basedpyright, pytest
- toute le temps des properties, pas d'acces directe
- faire ma nouvelle gestion d'erruer, avec le ErrorCode (NOTE PCO : changer le validate, et pouvoir passer un contexte, car quand je valide les lignes d'un tableau, je sais pas la ligne qui plante + faire comme systex avec un is_applicable et un tableau pour chainer la validationn comme mon pointeur de fonction)

Model, faire un héritage avec une des interfaces suivantes :
- Si CRUD, tout le temps date created, date modified, tout le temps ID, mark ascreateD/modified
- Si collection de CRUD, comme StepsCollection 
- to_dict, from_dict, clear (pas de default, le default c'est service qui crée), validate
- copy fonctionnel, copy technique
- fieldnames -> retourne toutes les clés/attributs (pas les valeurs)

Le service :
???

Le ViewModel aussi :
- faire une méthode 'snapshot' qui convertit l'UX/UI en model
- set_enabled -> va DISABLED UX/UI
- un show_error (j'ia toujours un show error, soit display une notif, soit MAJ le texte d'un lavel, soit affiche une popup modal, etc...)
- un clear qui vide l'IHM (les valeurs, etc.)
- faire un refresh qui prends un enum (et sélectionne qui refrachit quoi)
- faire un is_dirty (si l'utlisateur a changé l'IHM)
- ainsi qu'un is_busy (lorque l'utlilsateur fait un calcul)
- et un is_loading (important, des fois je change plusieurs variable, ce qui déclenche des traces, charges models, alorsque je pourrais grouper, et faire qu'un seul refresh à la fin, et donc je me dois d'ignorer 

- Interdiction de faire des '.open(' pour lire un fichier en dehors d'un repository

View :
- optimser avec viewport
- recycler ce qui est dessiné


-----------------------------------------

Pour le clique : Je suis passé en JS direct.
Les autres ne servent jamais à rien.
Mais peut être qu'il faudrait que je remette : Normal, forced, JS direct.
Pour l'instant, pas besoin.

-----------------------------------------

Vérification, improve cette aspect.
les steps, vérifier à l'ouverture de l'application tous les scéanrios
durant le spashscreen.
vérifier aussi à la sauvegarde la cohérence globale (et non plus unitairement)
Ne pas corriger la donnée, le passer en broken, et afficher une erreur en rouge.

---------------

C_LIMIT_GIVE_UP_SCROLLING 
2 fois
Est en dur
faudrait le rendre configurable dans le ScrollDown* (IHM, model, etc..)

-----------------------------------------

lorqu'on edit
sauvegarder dasn un fichier temporaire à chaque modif
et ainsi ne pas perdre le travail
Puis au moins, lorque je relance, je peux vérifier un travail a mal été save.
Une fois sauvegarder ou annulation complète, retirer le fichier.

---------------

Au lieu de faire une truc débile qui suffixe un ID pour le profil ou le scénario
Faire simplement un dossier.
Et comme ça, si delete id
delete tous ceux qui sont dans le dossier (et plus de probleme de match)

---------------

timeout en dur :
"socket_timeout": 30,
Pas bien.

---------------

Si un jour prolbème d'optimisation JSON car j'en lit beaucoup :
msgspec Struct (zéro-copie)
msgspec  (sérialisation/désérialisation zéro-copie via Struct)
Le plus rapide : schema défini → décodage typé en une passe.
cf internet

---------------

Faire la config en singleton.
Et tout le monde pioche dedans.
Vraiment chiant de MAJ toutes les couches.

---------------

si je supprime un scéario,
je supprime tous les profils,
mais je préviens pas du nombre de profil, pourtant il peut y avoir des dépendances.
le truc c'est que j'ai plein d'URL, et je peu xpas les réassinger.
c'est dommage, même siu pas grave de les perdre.

---------------

erreur inline
un jour, faire comme EC SAL :
erros_lines.py
En profiter aussi pour faire un composant générique "Ligne" qui permet de push des contrôles
ensuite je push une ligne
ovveride tout tkinter à force.

---------------


Brique supplémentaire à ajouter, mais non prioritaire :

- Take screenshot
prendre un screenshot. NOTE PCO s'en servir comme debug de ce qui palnte ?
page.screencast.on('screencastFrame', data => {
  console.log('received frame, jpeg size:', data.length);
});

- Focus visuel sur un élément.
rendre visible un élément et le focus (mettre focus dessus)
pratique poru les trucs hors champ qui peuvent se load uniquement lorsque visible
(car le JS se déclenche que si dans le viewport)


- Scroll aléatoire, pour masquer présence ?
scroll aléatoire humain pour simuler comportement ératique

-----------------------------------------


Pour le FUN :
- Fichier de configuration, et dedans, mettre la taille de la police et la typo.
- Pouvoir la modifier, genre augmenter la taille, commen dans les jeux vidéo.

---------------

Lorsque je fais un edit :
toujours créer un fichier temp, et ensuite rename.
comme ça, si echec durant l'écriture, ou l'application se quitte.
je ne perds pas le fichier, et il reste planté sur l'ancien.
En plus, si je reprends, je peux regarder s'il reste un travail en cours.

---------------

Niveau de sécurité d'un scenarios
NOTE PCO : Si je fais une bdd, ça anonymuse, et en vrai, python 3 fait SQL
Par contre, les scenarios,on pourrait imaginer :
- Niveau C1 : publique, tout circule
- Niveau C2 : obfuscation légère (lisible, donc éditable, mais des faux positifs). Un enfant de 10 ans peut le changer.
- Niveau C2 : obfuscation moyenne (base64 ou un truc du genre). Un adolescent de 16/20 peut le changer.
- Niveau C3 : crypté avec paswword : 
- Niveau C5 : Rien n'est save, le truc reste actif uniquement tant que le logiciel est présnet.

-----------------------------------------

Truc qui bypass cloudflare ?
Difficile à couvrir tous les challenge (les basics marchent, mais de ce que j'ai lu, les vérifs serveur sont tout de même très efficace...).
Sans parler des rate-limit, vérification des délais d'input sur un clavier, etc...
Par se cloudflare ici, simplement de la vérif front ou backend.
Comme les trucs qui vnt vérifié les mouvements de la souris pour voir si le truc se téléporte.

Par contre, cohabitation étrange avec playwright parfois
faut passer les cookies, la session etc, genre les 2 trucs sont étanches, mais ça fonctionne
L'IA semble arriver à le gérer.
https://github.com/VeNoMouS/cloudscraper
https://github.com/FlareSolverr/FlareSolverr

---------------




# EOF
